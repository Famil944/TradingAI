# 🤖 Crypto Spot Signal Bot

Телеграм-бот для анализа криптовалютного SPOT-рынка и отправки торговых сигналов.

## ✨ Главные возможности

- 🎯 **Анализ точек входа** после снижения цены
- 📊 **Технические индикаторы**: RSI, MACD, Bollinger Bands, ATR, EMA
- 💡 **Расчёт целей** (+3%, +5%, +8%, +15%) и Stop-Loss
- 📈 **Отслеживание результатов** сигналов
- 🔄 **Автоматическое сканирование** рынка каждые 5 минут
- ⚙️ **Персональные настройки** для каждого пользователя
- 🛡️ **Защита от спама** с Cooldown механизмом

## 📋 Требования

- Python 3.9+
- Telegram Bot Token
- Интернет подключение

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repo-url>
cd TradingAI
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```dotenv
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
BINANCE_BASE_URL=https://api.binance.com
SCAN_INTERVAL_MINUTES=5
MIN_SIGNAL_SCORE=75
SIGNAL_COOLDOWN_MINUTES=45
SIGNAL_SCORE_CHANGE_THRESHOLD=10
SCANNER_TOP_LIMIT=50
MIN_QUOTE_VOLUME_USDT=5000000
MIN_DRAWDOWN_PERCENT=3
MAX_DRAWDOWN_PERCENT=45
SCANNER_CONCURRENCY=8
DATABASE_URL=sqlite+aiosqlite:///./data/bot.db
LOG_LEVEL=INFO
```

### 5. Запуск бота

```bash
python main.py
```

Бот должен вывести:

```
2026-08-16 12:00:00 - __main__ - INFO - Инициализация базы данных...
2026-08-16 12:00:00 - __main__ - INFO - ✅ База данных инициализирована
2026-08-16 12:00:00 - __main__ - INFO - 🚀 Бот запущен! Ожидание сообщений...
```

## 📚 Команды Telegram

| Команда | Описание |
|---|---|
| `/start` | Запуск бота и описание |
| `/signals` | Лучшие текущие сигналы (Top-5) |
| `/top` | TOP-10 монет по Score |
| `/coin <TICKER>` | Подробный анализ пары (напр. `/coin SOL`) |
| `/scan` | Принудительное сканирование рынка |
| `/history` | История последних 20 сигналов |
| `/stats` | Статистика эффективности сигналов |
| `/settings` | Персональные настройки |
| `/help` | Список всех команд |

## 🎯 Система Score (0-100)

Сигнал создаётся на основе комбинации факторов:

| Фактор | Макс. баллов | Условие |
|---|---|---|
| RSI | 20 | RSI < 30 (+20) или RSI < 40 (+10) |
| Поддержка | 15 | Цена рядом с поддержкой (≤1% расстояния) |
| MACD | 15 | Ослабление падения или bullish crossover |
| Объём | 10 | Объём > среднего на 30%+ |
| Bollinger Bands | 10 | Цена в нижней части диапазона |
| EMA | 10 | Цена выше EMA20 и/или EMA разворачивается |
| **Base Score** | **40** | Начальное значение для всех сигналов |

### Отправка сигналов

| Score | Статус | Отправка |
|---|---|---|
| 0-39 | Плохой | ❌ Не отправляется |
| 40-59 | Слабый | 📝 Только по команде |
| 60-74 | Средний | 📝 Только по команде |
| 75-84 | Хороший | ✅ Автоматически |
| 85-100 | Отличный | ✅ Автоматически |

## 📊 Формат сигнала

Пример сигнала в Telegram:

```
💰 SOL/USDT | Score: 82/100

Цена: $145.20

🎯 Возможная зона входа:
$143.80 – $145.50

Цели:
TP1: $149.56 (+3%)
TP2: $152.46 (+5%)
TP3: $156.82 (+8%)
TP4: $166.98 (+15%)

🛑 Stop: $141.70 (-2.4%)

📊 R/R: 1:2.1
RSI 5m: 31
Объём: +42%

Причины:
✅ цена рядом с поддержкой
✅ RSI выходит из перепроданности
✅ MACD показывает ослабление падения
```

## 🔒 Защита от спама (Cooldown)

- **По умолчанию**: 45 минут между сигналами одной пары
- **Исключение**: если Score изменился > ±10 пунктов, сигнал отправляется сразу
- Параметры настраиваются в `.env`:
  - `SIGNAL_COOLDOWN_MINUTES` - время ожидания
  - `SIGNAL_SCORE_CHANGE_THRESHOLD` - порог изменения Score

## 🗄️ База данных

SQLite база данных сохраняется в `data/bot.db` и содержит:

- **signals** - все найденные торговые сигналы
- **signal_results** - результаты отслеживания сигналов
- **user_settings** - настройки пользователей
- **signal_cooldown** - информация о последних сигналах (для cooldown)

## 🧪 Тестирование

Запуск всех тестов:

```bash
python -m pytest tests/
```

Или конкретные тесты:

```bash
# Тесты индикаторов
python -m unittest tests.test_indicators

# Тесты скоринга
python -m unittest tests.test_scoring
```

## 📈 Статистика сигналов (`/stats`)

Бот отслеживает результаты сигналов и показывает:

- Всего сигналов
- Количество сигналов, достигших TP1/TP2/TP3/TP4/STOP
- Среднее максимальное движение (MFE)
- Средний Score выигрышных и проигрышных сигналов
- Разбивка по диапазонам Score (75-79, 80-84, 85-89, 90+)
- Статистика по монетам и времени

## ⚠️ ВАЖНЫЕ ОГРАНИЧЕНИЯ

### ✅ ЧТО ДЕЛАЕТ БОТ

- Только анализирует рынок SPOT
- Публичный API (без API-ключей)
- Отправляет сигналы в Telegram
- Отслеживает результаты

### ❌ ЧТО НЕ ДЕЛАЕТ БОТ

- **НИКОГДА** не создаёт ордера
- **НИКОГДА** не покупает/продаёт
- **НИКОГДА** не требует торговые API-ключи
- **НИКОГДА** не хранит личные данные

## 📢 Disclaimer

> **Сигналы являются результатом технического анализа и не являются гарантией роста цены или финансовой рекомендацией.**
>
> Торгуйте на свой риск! Криптовалютный рынок волатилен и может привести к потерям.

## 🛠️ Архитектура проекта

```
app/
├── analysis/
│   ├── __init__.py
│   ├── indicators.py         # Расчёт индикаторов
│   ├── support_resistance.py # Уровни поддержки/сопротивления
│   └── signal_scorer.py      # Логика скоринга
├── bot/
│   ├── __init__.py
│   └── commands.py           # Команды Telegram
├── config/
│   ├── __init__.py
│   ├── models.py             # Pydantic модели
│   └── settings.py           # Настройки приложения
├── database/
│   ├── __init__.py
│   └── db.py                 # SQLite база данных
├── exchange/
│   ├── __init__.py
│   └── binance_client.py     # Binance API клиент
├── services/
│   ├── __init__.py
│   └── scanner.py            # Фоновый сканер рынка
├── tests/
│   ├── __init__.py
│   ├── test_indicators.py    # Тесты индикаторов
│   └── test_scoring.py       # Тесты скоринга
├── main.py                   # Точка входа
├── requirements.txt          # Зависимости
├── .env.example              # Шаблон переменных окружения
└── README.md                 # Этот файл
```

## 🚀 Развёртывание на Render

### 1. Пушим код на GitHub

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

### 2. Создаём новый проект на Render

- Выбираем "New" → "Web Service"
- Подключаем GitHub репозиторий
- Build command: `pip install -r requirements.txt`
- Start command: `python main.py`
- Добавляем переменные окружения в "Environment"

### 3. Развёртываем

Render автоматически запустит бота после развёртывания.

## 🐛 Отладка

Включите DEBUG логирование в `.env`:

```dotenv
LOG_LEVEL=DEBUG
```

Логи сохраняются в консоль и можно просматривать в Render Dashboard.

## 📝 Лицензия

MIT License - смотрите LICENSE файл для деталей

## 👥 Контрибьютинг

Contributions are welcome! Please feel free to submit a Pull Request.

## 💡 Идеи для расширения

- [ ] Поддержка дополнительных бирж (Bybit, OKX)
- [ ] Веб-панель мониторинга
- [ ] AI-анализ паттернов
- [ ] Интеграция с Redis для кэширования
- [ ] PostgreSQL вместо SQLite
- [ ] Экспорт отчётов в Excel/PDF
- [ ] Telegram канал для публичных сигналов
- [ ] Уведомления в Discord

Telegram-бот для анализа рынка, локальной Paper-торговли и торговли на
Binance Futures Demo/Testnet.

## Безопасность

- По умолчанию используется `TRADING_MODE=DEMO`.
- `DEMO` отправляет настоящие ордера в Binance Futures Demo, а не симулирует
  их локально.
- Локальная симуляция находится в модуле `paper`.
- Live-ордера разрешаются только при `TRADING_MODE=LIVE`, наличии live-ключей
  и точном значении
  `LIVE_TRADING_CONFIRMATION=I_UNDERSTAND_LIVE_RISK`.
- Не храните `.env` и API-ключи в Git.

## Установка

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e .
Copy-Item .env.example .env
```

Заполните `.env`, затем запустите:

```powershell
.\.venv\Scripts\python.exe preflight.py
.\.venv\Scripts\python.exe main.py
```

`preflight.py` не отправляет ордера: он проверяет ключи, соединение, серверное
время, разрешение торговли, One-way Mode и фильтры инструмента.

Для первого теста установите:

```dotenv
TRADING_MODE=DEMO
TRADING_CLOSE_ONLY=false
```

Команды `/demo_long` и `/demo_short` работают только в Demo. Команды
`/trade_long` и `/trade_short` используют текущий режим. `/trading_mode`
показывает активный режим и состояние Live-блокировки.

Режим также можно выбрать через Telegram:

```text
🏠 Меню → ⚙️ Настройки → 🔁 Demo / Live
```

Переключатель проверяет отсутствие открытых позиций, ключи и соединение.
Для LIVE дополнительно показывается подтверждение риска. После успешной
проверки режим и биржевой клиент меняются сразу, без перезапуска.

Live-контур использует Binance Portfolio Margin UM (`/papi`). Для Live
необходимы все три настройки:

```dotenv
TRADING_MODE=LIVE
BINANCE_LIVE_API_KEY=...
BINANCE_LIVE_API_SECRET=...
LIVE_TRADING_CONFIRMATION=I_UNDERSTAND_LIVE_RISK
```

Для аварийной блокировки новых входов установите
`TRADING_CLOSE_ONLY=true`: закрытие
позиций, отмена ордеров и установка защиты при этом остаются доступны.

Аккаунт Binance должен использовать **One-way Mode**. При Hedge Mode или
расхождении системного времени с Binance более чем на пять секунд запуск
будет остановлен до исправления конфигурации.

API-ключ Live должен иметь только `Enable Reading` и
`Enable Portfolio Margin Trading`, а также ограничение по доверенному IP.
Вывод средств, Universal Transfer и займы боту не нужны.

## Настройки в Telegram

Раздел `⚙️ Настройки` позволяет без изменения файлов выбрать риск на сделку,
максимальный размер позиции, минимальный Quality Score, таймфрейм
автосканирования и режим уведомлений. Значения хранятся в SQLite и применяются
к следующему анализу или сделке.

По умолчанию включён консервативный авто-риск: бот уменьшает заданный верхний
лимит риска для менее качественных сигналов и дополнительно ограничивает
позицию 20% доступного баланса. При рестарте Render включённая автоторговля
возобновляется автоматически и продолжает отправлять уведомления в последний
Telegram-чат, из которого она была включена.

Профиль по умолчанию рассчитан на малый счёт около 100 USDT:

- риск на сделку не выше 0,5%;
- плечо не выше 2x;
- максимум две одновременные позиции;
- дневной лимит убытка 2%;
- остановка после двух последовательных убытков;
- минимальный Quality Score 75;
- 30-минутный cooldown после сделки по тому же символу.

Автоторговля использует консервативный белый список ликвидных криптовалют из
`config/trading_universe.py`. TradFi-контракты и произвольные новые тикеры из
биржевого списка автоматически не торгуются.

Кнопка `🛑 Аварийная остановка` немедленно и с сохранением состояния запрещает
новые автоматические входы. Она намеренно не отменяет защитные ордера и не
останавливает мониторинг уже открытых позиций.

## Render

В корне репозитория находится `render.yaml`: он создаёт платный Background
Worker и постоянный диск `/var/data`. В Render выберите **New → Blueprint**,
подключите репозиторий и заполните предложенные секретные переменные.

Перед первым запуском:

1. Оставьте `TRADING_MODE=DEMO`. Blueprint включает Demo-сделки значением
   `TRADING_CLOSE_ONLY=false`; для проверки только чтения временно поставьте
   `true`.
2. Заполните Telegram token и Demo-ключи.
3. После создания сервиса откройте `Connect → Outbound` и добавьте все
   исходящие IP-диапазоны сервиса в whitelist Binance.
4. Остановите локальную копию бота: Telegram polling допускает один активный
   экземпляр с этим токеном.
5. Проверьте баланс, режим One-way и защитные ордера на первой Demo-сделке.

SQLite, состояние автоторговли и выбранный через Telegram режим сохраняются
на диске. Секреты задаются только в Render Environment и не должны попадать
в Git.

## Проверка

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

Перед Live-режимом обязательно проведите длительную проверку на Binance Demo,
сверьте фильтры количества/цены каждого инструмента и убедитесь, что защитные
ордера присутствуют непосредственно на бирже.

Каждая открытая позиция получает биржевые `STOP_MARKET` и
`TAKE_PROFIT_MARKET`. При рестарте и перед каждым автоматическим сканированием
бот сверяет позицию, открытые ордера и БД, восстанавливает отсутствующую
защиту и фиксирует закрытия, выполненные биржей. Закрытый PnL учитывает
биржевые комиссии и funding.
